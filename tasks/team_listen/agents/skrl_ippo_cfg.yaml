# =========================================================================
# COPIED per M1_SPEC 0 / 1.14 from the PINNED Isaac Lab checkout
#   ~/IsaacLab @ b0542fe2d (main, 2026-07-24), Isaac Sim 5.1.0, skrl 2.1.0
#   source/isaaclab_tasks/isaaclab_tasks/direct/cart_double_pendulum/
#       agents/skrl_ippo_cfg.yaml
# on 2026-08-31, key set preserved (agent/memory/trainer/seed identical;
# models.policy keys necessarily differ: CategoricalMixin replaces the
# example's GaussianMixin kwargs), values per the spec 1.14 block.
#
# M1_SPEC section 0 / 1.14 PROCEDURE (blocking, machine-checked):
#   Before first training on the 5090, RE-COPY this file VERBATIM from the
#   PINNED Isaac Lab checkout:
#     ~/IsaacLab/source/isaaclab_tasks/isaaclab_tasks/direct/
#         cart_double_pendulum/agents/skrl_ippo_cfg.yaml
#   and then re-apply ONLY the deliberate deviations listed below.
#   tests/test_yaml_keyset.py diffs this file's KEY SET against the pinned
#   checkout's and must pass: identical keys, only values differ.
#
# WHY (spec 1.14 [FIXED: *_preprocessor_kwargs]): skrl's Runner._generate_agent
#   injects preprocessor size/device via
#     agent_cfg.get("<name>_preprocessor_kwargs", {}).update({...})
#   On an ABSENT key .get returns a throwaway dict and the injection is
#   silently discarded; only a key-present-but-null entry is rescued by
#   _process_cfg. Every explicit `null` below is therefore load-bearing.
#
# DELIBERATE DEVIATIONS from the shipped cart_double_pendulum values
# (re-apply these, and ONLY these, after the verbatim re-copy):
#   * agent.entropy_loss_scale: 0.008   -- the shipped 0.0 collapses a
#     deliberately symmetric 5-way Categorical to a fixed left/right
#     convention early: the exact degeneracy the audit exists to detect.
#   * agent.observation_preprocessor / agent.state_preprocessor: null
#     (features are hand-normalised; RunningStandardScaler on the
#     near-degenerate 32-d language slice amplifies numerical noise --
#     spec OPEN(3)). value_preprocessor stays RunningStandardScaler: it
#     acts on returns, never on observations.
#   * agent.time_limit_bootstrap: true  -- is_finite_horizon = False
#     truncation bootstrap (spec 1.4).
#   * trainer.timesteps -- set by the section 8 training-FPS gate.
#   * agent.discount_factor MUST stay equal to cfg.reward_gamma = 0.99
#     (the gamma-correct potential shaping of spec 1.12 depends on it).
#
# NOT bugs, do NOT "fix" (spec 0 [FIXED: bogus renames]):
#   * `lambda` is the correct key on skrl 1.4.x AND 2.x
#     (Runner._check_cfg_compatibility maps it to gae_lambda).
#   * the `models:` block is FLAT per-role on 2.x too
#     (Runner._generate_models copies it per agent id).
# =========================================================================
#
# IPPO-SPECIFIC CHOICES (spec gives no ippo block; flagged for review):
#   * models.separate: True and 512-256-128 nets, mirroring the MAPPO file
#     (shipped example uses separate: False with 32-32); value input is
#     OBSERVATIONS (IPPO has no central state).
#   * state_preprocessor is null in the SHIPPED ippo yaml already; kept.
#   * used on the partial-observation arms (Lang, SymbolPO, Mute) as the
#     paired control (spec 1.14 [FIXED: vacuous MAPPO-vs-IPPO check]).
#

seed: 42


# Models are instantiated using skrl's model instantiator utility
# https://skrl.readthedocs.io/en/latest/api/utils/model_instantiators.html
models:
  separate: True
  policy:  # see categorical_model parameters
    class: CategoricalMixin
    unnormalized_log_prob: True
    network:
      - name: net
        input: OBSERVATIONS
        layers: [512, 256, 128]
        activations: elu
    output: ACTIONS
  value:  # see deterministic_model parameters
    class: DeterministicMixin
    clip_actions: False
    network:
      - name: net
        input: OBSERVATIONS
        layers: [512, 256, 128]
        activations: elu
    output: ONE


# Rollout memory
# https://skrl.readthedocs.io/en/latest/api/memories/random.html
memory:
  class: RandomMemory
  memory_size: -1  # automatically determined (same as agent:rollouts)


# IPPO agent configuration (field names are from IPPO_DEFAULT_CONFIG)
# https://skrl.readthedocs.io/en/latest/api/multi_agents/ippo.html
agent:
  class: IPPO
  rollouts: 16
  learning_epochs: 5
  mini_batches: 4
  discount_factor: 0.99                # MUST equal cfg.reward_gamma (spec 1.12)
  lambda: 0.95
  learning_rate: 3.0e-04
  learning_rate_scheduler: KLAdaptiveLR
  learning_rate_scheduler_kwargs:
    kl_threshold: 0.008
    max_lr: 1.5e-4  # ratified cap v2 (DECISIONS.md 2026-09-01, pilot probe): 5e-4 removed only
                    # ~4x of the crash hazard and left the mid-run degradation; 1.5e-4 (0.5x base LR)
                    # eliminated both in the pilot probe (docs/PILOT_RESULTS.md section 4)
  observation_preprocessor: null
  observation_preprocessor_kwargs: null
  state_preprocessor: null
  state_preprocessor_kwargs: null
  value_preprocessor: RunningStandardScaler
  value_preprocessor_kwargs: null
  random_timesteps: 0
  learning_starts: 0
  grad_norm_clip: 1.0
  ratio_clip: 0.2
  value_clip: 0.2
  clip_predicted_values: True
  entropy_loss_scale: 0.008          # NOT the 0.0 default (see header)
  value_loss_scale: 2.0
  kl_threshold: 0.0
  rewards_shaper_scale: 1.0
  time_limit_bootstrap: True
  # logging and checkpoint
  experiment:
    directory: "runs"
    experiment_name: ""
    write_interval: auto
    checkpoint_interval: auto


# Sequential trainer
# https://skrl.readthedocs.io/en/latest/api/trainers/sequential.html
trainer:
  class: SequentialTrainer
  # 150 M env steps (spec 4.2) at num_envs = 8192, frozen by the section
  # 8.2 sweep (docs/GATE_RESULTS.md: training-FPS maximum, gate passed at
  # 390k env-steps/s so no reduction ladder).  150e6 / 8192 = 18310.55;
  # rounded DOWN to the nearest multiple of rollouts (16) so the run ends
  # on an update boundary: 18304 * 8192 = 149,946,368 env steps (~149.9M).
  timesteps: 18304
  environment_info: log
